const fs=require('fs');const path=require('path');const dir='C:/Users/GAMING PC/.gemini/antigravity/scratch/enterprise-task-system/public/pages';const files=fs.readdirSync(dir).filter(f=>f.endsWith('.html'));const missing=new Set();files.forEach(f=>{const html=fs.readFileSync(path.join(dir,f),'utf8');const matches=html.match(/>([^<]+)</g);if(matches){matches.forEach(m=>{let text=m.replace(/[><]/g,'').trim();if(text&&/[a-zA-Z]/.test(text)){missing.add(text);}})}});fs.writeFileSync('missing.json', JSON.stringify([...missing], null, 2));
